package obj;

import obj.Line;

class InterLines {
	public var inside:Line;
	public var outside:Line;

	public function new(_inside:Line, _outside:Line) {
		inside = _inside;
		outside = _outside;
	}
}
